language: python

python:
  - "2.7"

env:
  - DJANGO_VERSION=1.5.8
  - DJANGO_VERSION=1.6.0
  - DJANGO_VERSION=1.7.0


install:
  - pip install -q Django==$DJANGO_VERSION --index-url
  - pip install -q -e . --use-mirrors

script: python setup.py test

branches:
  only:
    - dev
